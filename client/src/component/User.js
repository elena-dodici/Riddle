/**
 *
 * @param {number} id the code of the history
 * @param {string} name name of user
 * @param {number} point point of user


 */

function User(id, name, point) {
  this.id = id;
  this.name = name;
  this.point = point;
}

export default User;
